#!/usr/bin/env bash

set -eu

# Rancher Desktop (containerd/dockerd + kubectl + nerdctl)
brew install --cask rancher

echo "Rancher Desktop installed. Launch it from Applications to complete setup."
echo "Set container engine to 'dockerd (moby)' in Preferences if you want docker CLI compatibility."
